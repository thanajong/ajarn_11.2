#include<stdio.h>
int main()
{
	float a,b,c;
	scanf("%f %f %f",&a,&b,&c);
	if(a<=c&&b<=c)
	{	
		printf("%f",a+b);
	}
	else if(a<=b&&c<=b)
	{	
		printf("%f",a+c);
	}
	else if(b<=a&&c<=a)
	{	
		printf("%f",a+c);
	}
	return 0;
}
